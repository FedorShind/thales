# Open questions

Strict append-only list. Same format as ruled_out.md. Mark blocking questions explicitly in the reason field:

```
[YYYY-MM-DD HH:MM | iter-N | confidence: high|med|low] <question> — blocking: <why> | non-blocking: <why file for future>
```

Judge uses blocking entries to gate convergence.
