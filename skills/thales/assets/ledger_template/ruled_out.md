# Ruled-out claims

Strict append-only list. One line per entry. Format:

```
[YYYY-MM-DD HH:MM | iter-N | confidence: high|med|low] <one-line claim> — <one-line reason>
```

Entries must not be summarized when included in subagent briefs. Always paste verbatim.
