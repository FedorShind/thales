# Abandoned branches

Strict append-only list. Same format as ruled_out.md, but the "claim" is a branch direction. One line per entry:

```
[YYYY-MM-DD HH:MM | iter-N | confidence: high|med|low] branch-N (<short name>) — <one-line reason for abandonment>
```
